Match found:
  <span class="meta-value" id="meta-nationality">奥地利</span>

          </div>

          <!-- Large spinning planet canvas -->

          <canvas id="poster-canvas" width="420" height="420"></canvas>

          <!-- Bottom Left -->

          <div class="poster-meta-item bl">

            <span class="meta-label">☀️ PRIORITY LEVEL // 考察频次</span>

            <span class="meta-value" id="meta-prior
----------------------------------------
Match found:
nectionLines(s) {

  // 根据用户要求，完全关闭科学家之间的任何关系连线

  connectionLines = [];

}

function renderPosterPlanet(s) {

  const pcv = document.getElementById('poster-canvas');

  if (!pcv) return;

  const pctx = pcv.getContext('2d');

  const w = pcv.width;

  const h = pcv.height;

  pctx.clearRect(0, 0, w, h);

  const cx = w / 2;

  const cy = h / 2;

  const r = w / 2 - 35;

  pctx.save();

  // 1. 轨道
----------------------------------------
